# Nintendo game data — never commit
*.iso
*.gcm
*.ciso
*.dat
*.usd
*.dol
gamedata/

# Third-party clones (re-create with setup commands in README)
external/

# Python
.venv/
__pycache__/
*.pyc

# .NET
bin/
obj/
*.user
.vs/

# Machine-specific paths (ISO location) — never commit
local.json
HANDOFF_PROMPT.md

# Posed hurtbox exports are ~500 MB total; regenerate with tools/ShieldPose
data/*_hurtboxes.csv
# ad-hoc pose dumps
*_posedump*.csv

# Web export (skeleton + raw Guard FObj bytes); regenerate with ShieldPose --export-web
data/shieldpose_web.json
